name: dps_core_package
description: A starting point for Dart libraries or applications.
version: 1.0.0
homepage: http://localhost:8081/repository/pub-group
publish_to: "http://localhost:8081/repository/pub-hosted"

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies: {}
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
